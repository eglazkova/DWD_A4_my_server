
const quotes = require("quotesy");
const randomColor = require('randomcolor'); 
const color = randomColor(); 






function getColorQuote() {
 console.log(getColorQuote());
 const quote = quotes.random(); 
    return `${quote} ${color}`;
  }
  console.log(getColorQuote());